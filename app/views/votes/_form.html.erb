<%= form_for(@vote) do |f| %>
  <% if @vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
      <% @vote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number_of_votes %><br />
    <%= f.number_field :number_of_votes %>
  </div>
  <div class="field">
    <%= f.label :electoral_district %><br />
    <%= f.text_field :electoral_district %>
  </div>
  <div class="field">
    <%= f.label :committee %><br />
    <%= f.text_field :committee %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
